# CMakeLists.txt

## x-qt-plugin Support
IF (QT_QTDESIGNER_FOUND AND QT_QTDESIGNERCOMPONENTS_FOUND)
  ADD_SUBDIRECTORY(passworddialog)
ENDIF (QT_QTDESIGNER_FOUND AND QT_QTDESIGNERCOMPONENTS_FOUND)

## EOF
